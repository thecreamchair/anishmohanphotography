import blockContent from './blockContent'
import portfolio from './portfolio'
import selectedWork from './selectedWork'
import blog from './blog'

export const schemaTypes = [blockContent, portfolio, selectedWork, blog]
